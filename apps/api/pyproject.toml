[project]
name = "phoenix-api"
version = "0.1.0"
description = "FastAPI orchestrator for Project Phoenix"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
    "pydantic>=2.13.4",
    "pandas>=2.3.3",
    "ml-core",
]

[tool.uv]
package = false
