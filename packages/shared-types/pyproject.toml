[project]
name = "phoenix-shared-types"
version = "0.1.0"
description = "Shared Pydantic schemas for Phoenix"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.13.4"]

[tool.uv]
package = false
